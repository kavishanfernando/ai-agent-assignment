[tool]
name = "ai-agent-system"
version = "0.1.0"
description = "An AI-assisted agent-based software solution."
authors = ["Your Name <your.email@example.com>"]
license = { file = "LICENSE" }

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
python = "^3.8"
flask = "^2.0"
numpy = "^1.21"
pandas = "^1.3"
scikit-learn = "^0.24"
requests = "^2.26"

[tool.poetry.dev-dependencies]
pytest = "^6.2"
black = "^21.7b0"
mypy = "^0.910"
flake8 = "^3.9"